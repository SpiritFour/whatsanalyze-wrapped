name: Dev Deploy (Firebase Hosting)

# This is the DEV deployment - automatically deploys on merge to production branch
# For PROD deployment (GitHub Pages), use the manual "Production Deploy" workflow
on:
  push:
    branches:
      - production

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Frontend
        uses: ./.github/actions/build-frontend
        with:
          env: dev

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_WHATSANALYZE_WRAPPED }}
          channelId: live
          projectId: whatsanalyze-wrapped
